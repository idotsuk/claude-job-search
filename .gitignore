# Personal config — NEVER commit
config.yaml
config.*.yaml
!config.yaml.example
standard-answers.md

# All run state (listings, runs, reports, registry, browser sessions)
data/

# Cruft
__pycache__/
*.pyc
.DS_Store

# Playwright MCP runtime cache/logs (accumulates at repo root when the MCP
# server's cwd is here — not repo content)
.playwright-mcp/
